- include_stylesheet('admin/multi_site')
- render_region :main do |main|
  - main.edit_header do
    %h1 Edit Role
  - main.edit_form do
    - form_for :role, @role, :url => admin_role_path(@role), :html => {:method => :put} do |f|
      = render :partial => "form", :locals => {:f => f}
